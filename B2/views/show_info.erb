<div class="container"style="padding: 200px 300px 100px;">
	<div class="jumbotron">
<h3>添加的留言如下:<h3>
<p><h3>留言的id:<%= @message.id %></h3>
<p><h3>留言时间:<%= @message.created_at %></h3>
<p><h3>留言作者:<%= @message.user_id %></h3>
<P><h3>留言内容：
  <div>

		<textarea  name="content" > <%= @message.content %> </textarea></h3>
    <p>
    <h3><a href="/show/infos">确定保存</a>
    <a href="/show/new">返回</a></h3>
	</div>


</div>
</div>


<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>
<p>

<p>
  <strong>Author:</strong>
  <%= @post.user.username %>
</p>

<p>
  <strong>Published at:</strong>
  <%= @post.published_at %>
</p>


<p>


</p>

<h2>comments</h2>
<% @post.comments.each do |comment| %>
  <p>  <strong>author:</strong>
    <%= @post.user.username %>
    <p>
      <strong>content:</strong>
       <%= comment.content %>

<% end %>

<p>
  <h2>add a comment</h2>
  <%= form_for([@post,@post.comments.build]) do |f| %>
  <p>

    <%= f.label :content%>
    <%= f.text_area :content %>
    <p>
      <%= f.submit %>
      <p>
        <% end %>
<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
